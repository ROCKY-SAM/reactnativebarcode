class Barcode {
  constructor(id, barcode, vendor, itemtype,price) {
    this.id = id;
    this.barcode = barcode;
    this.vendor = vendor;
    this.itemtype = itemtype;
    this.price = price;
  }
}
export default Barcode;